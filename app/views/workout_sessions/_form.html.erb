<%= semantic_form_for @workout_session do |f| %>
  <!-- <ul>
  <% @workout_session.errors.full_messages.each do |error| %>
    <li><%= error %></li>
  <% end %>
</ul> -->

    <!--  To be included for dynamic content:
      <div class="input-group">
        <div class="input-group-btn">
          <button type="button" class="btn btn-default dropdown-toggle form-control" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Main Workout Focus <span class="caret"></span></button>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#">Cardio</a></li>
            <li><a href="#">HIIT</a></li>
            <li><a href="#">Circuit</a></li>
            <li><a href="#">Lower Body</a></li>
            <li><a href="#">Upper Body</a></li>
          </ul>
        </div> -->

    <!--Overall Workout Information -->
    <div class="row">

        <h2>Overall Workout Information</h2>
        <%= f.inputs do %>
          <%= f.input :workout_focus, :label => "Focus" %>
          <%= f.input :workout_time, :label => "Overall Time"%>
          <%= f.input :workout_intensity, :label => "Intensity" %>
          <%= f.input :workout_notes, :label => "Notes" %>
        <% end %>
        <hr>

    </div>

    <!-- Exercises -->
    <div class="row">
      <div class="col-md-10">
        <h2> Add Exercise </h2>
          <div id='exercises'>
            <%= f.semantic_fields_for :exercises do |exercise| %>
              <%= render 'exercise_fields', :f => exercise %>
            <% end %>
            <div class='links'>
              <%= link_to_add_association 'add', f, :exercises %>
            </div>
          </div>
      </div>
    </div>

  <center>
    <%= f.actions :submit, :cancel %>
  </center>
<% end %>
